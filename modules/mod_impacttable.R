impact_ui <- function(id){
  
  ns <- NS(id)
  
  fluidPage(
    fluidRow(),
    fluidRow()
  )
  

}


impact_server <- function(id, n_df, table_shell){
  
  moduleServer(id, function(input, output, session){
    
    
    
  })
  
}